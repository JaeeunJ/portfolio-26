{
  "paths": {
    "canvases": "./designs"
  },
  "scripts": {
    "install": "cd .. && npm install && cd tempo && npm install",
    "start": "npm run dev"
  },
  "apps": [
    {
      "appStart": "npm run dev -- --port=${PORT}"
    }
  ]
}
